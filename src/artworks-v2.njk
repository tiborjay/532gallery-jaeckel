---
layout: base-v2.njk
title: One-of-a-kind Artworks
permalink: "/artworks-v2/index.html"
showSearch: true
---

<div class="page">
<h1 class="page__title">ONE-OF-A-KIND ARTWORKS:</h1>


  <div class="grid">
    {% for artwork in artworks_v2 %}
      <article class="card">
        <a
          class="card__img"
          href="/artwork-v2/{{ artwork.slug }}/"
          aria-label="{{ artwork.artist }} — {{ artwork.title }}"
        >
          <img
            class="front"
            src="/assets/img/{{ artwork.images.front }}"
            alt="{{ artwork.artist }} — {{ artwork.title }}"
          >

          {% if artwork.images.hover %}
          <img
            class="hover"
            src="/assets/img/{{ artwork.images.hover }}"
            alt=""
            aria-hidden="true"
          >
          {% endif %}
        </a>

        <div class="meta">
          <div class="artist">{{ artwork.artist | upper }}</div>
          <div class="title">
            {{ artwork.title }}{% if artwork.year %}, {{ artwork.year }}{% endif %}
          </div>
          {% if artwork.medium %}
          <div class="medium">{{ artwork.medium }}</div>
          {% endif %}
          {% if artwork.price %}
          <div class="price">$ {{ artwork.price | price }}</div>
          {% endif %}
        </div>
      </article>
    {% endfor %}
  </div>
</div>
